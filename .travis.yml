language: cpp
compiler: 
  - gcc
  
sudo: false

addons:
  apt:
    sources:
    - boost-latest
    packages:
    - xsltproc
    - libxerces-c-dev
    - xsdcxx
    - libboost1.55

script:
  - mkdir build && cd build && cmake .. && make
